//* IMPORTANT GLOBAL: middle message index
let currentMiddleIndex = "no element";

export function getCurrentMiddleIndex() {
  return currentMiddleIndex;
}

export function setCurrentMiddleIndex(val) {
  currentMiddleIndex = val;
}
